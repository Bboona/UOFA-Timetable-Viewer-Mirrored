json.extract! activity, :id, :class_name, :subject, :class_code, :class_nbr, :class_type, :size, :available, :colour, :term, :weeks, :days, :hours, :created_at, :updated_at
json.url activity_url(activity, format: :json)
